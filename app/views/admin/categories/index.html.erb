<h1><%= current_user.nickname %></h1>

<ul>

	<% @categories.each do |category| %>	
		<li>

			<%= category.name %>
			<%= link_to 'edit', edit_admin_category_path(category) %>
			<%= link_to 'destory', admin_category_path(category), method: :delete %>

		</li>
	<% end %>

</ul>